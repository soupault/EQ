// Function    : D flip-flop async reset

module FF (d, clk, clrn, q);

input d, clk, clrn; 
output reg q;

always @ (posedge clk or negedge clrn)
	if (~clrn) begin
		q <= 1'b0;
	end else begin
		q <= d;
	end

endmodule
